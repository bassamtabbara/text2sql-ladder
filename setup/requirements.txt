# Pinned for reproducibility. Tested on a single H100 80GB (CUDA 12.x), Python 3.11.
# Bump deliberately and re-run the affected rungs if you do.

# build/runtime tooling (uv venvs omit these; bitsandbytes' transformers integration imports
# pkg_resources/setuptools at runtime)
setuptools>=70
wheel>=0.43

# core training / modeling
torch==2.5.1
transformers==4.48.0
datasets==3.2.0
accelerate==1.3.0
peft==0.14.0
trl==0.14.0
bitsandbytes==0.45.0

# fast LoRA/QLoRA (rung 2a)
unsloth==2025.1.5

# serving + eval client (rungs 0, 1, and all serving rungs)
vllm==0.6.6.post1
openai==1.60.1

# RAG retrieval (rung 0)
sentence-transformers==3.3.1

# NOTE: rung 1's Vertex SDK (google-cloud-aiplatform) is intentionally NOT here -- its grpcio/
# protobuf/api-core pins conflict with this GPU stack. It installs into a separate venv from
# setup/requirements-vertex.txt (handled automatically by rung1-vendor-ft/run.sh).

# misc
pyyaml==6.0.2
tqdm==4.67.1
